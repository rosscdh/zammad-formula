{%- from "zammad/map.jinja" import config with context %}
IMAGE_REPO={{ config.image | default('zammad/zammad-docker-compose') }}
POSTGRES_PASS={{ config.postgres.user | default('zammad') }}
POSTGRES_USER={{ config.postgres.pass | default('zammad') }}
RESTART={{ config.restart | default('always') }}
# don't forget to add the minus before the version
VERSION=-{{ config.zammad_version | default('3.4.0-4') }}
